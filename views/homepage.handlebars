{{#each posts as |post|}}
<h2>
  <a href="/post/{{post.id}}">{{post.title}}</a>
</h2>
<p class="post-body">
  {{post.post_body}}
</p>
<p class="post-footer">
  {{post.user_id}} on {{post.post_date}}
</p>
{{/each}}