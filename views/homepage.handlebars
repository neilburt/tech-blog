
{{#each posts as |post|}}
<div class="row mb-4 post">
  <div class="col-md-5">
    <h2>
      <a href="/posts/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>
      {{post.post_body}}
    </p>
    <p>Created by {{user.name}} on {{post.post_date}}</p>
  </div>
</div>
{{/each}}
