
{{#each posts as |post|}}
<div class="row mb-4 post">
  <div class="col-md-5">
    <h2>
      <a href="/posts/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>
      {{post.post_body}}
    </p>
    <p>Posted by {{user.name}} on {{format_date post.post_date}}</p>
  </div>
</div>
{{/each}}
