<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
</head>
<body>
  <header>
    <h1>Tech Blog</h1>
    <nav>
      {{#if logged_in}}
      <a href="/profile">profile</a> | <button id="logout">logout</button>
      {{else}}
      <a href="/login">login</a>
      {{/if}}
    </nav>
  </header>
  <main>
    {{{body}}}
  </main>
  {{#if logged_in}}
  <script src="./js/logout.js"></script>
  {{/if}}
</body>
</html>